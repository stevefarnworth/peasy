# frozen_string_literal: true

require 'peasy'

RSpec.describe Peasy do
  it "has a version number" do
    expect(Peasy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
